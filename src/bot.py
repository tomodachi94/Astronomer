# src/bot.py

import os #The one import outside of src/envir.py
os.system("python src/envir.py") #Runs src/envir.py to set functions
imports() #Imports deps
setallenv() #Creates variable for token and prefix